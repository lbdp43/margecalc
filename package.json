{
  "name": "margebar",
  "private": true,
  "workspaces": [
    "shared",
    "backend",
    "mobile"
  ],
  "scripts": {
    "build": "npm run build --workspace=shared && cd backend && npx prisma generate && cd .. && npm run build --workspace=backend",
    "start": "npm run start --workspace=backend"
  }
}
