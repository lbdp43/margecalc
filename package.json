{
  "name": "margebar",
  "private": true,
  "workspaces": [
    "shared",
    "backend",
    "mobile"
  ],
  "scripts": {
    "build": "npm run build --workspace=shared && cd backend && npx prisma generate && cd .. && npm run build --workspace=backend && cd mobile && npx expo export --platform web --output-dir ../backend/dist/public && cd ..",
    "start": "npm run start --workspace=backend"
  }
}
